# Dominio publicado — usado en sitemap, OG, manifest
NEXT_PUBLIC_SITE_URL=https://pokehub.vercel.app

# Supabase (auth + cloud save de equipos/favoritos) — opcional
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Anthropic API — opcional, activa /api/recommend (sugerencias IA)
ANTHROPIC_API_KEY=
